node_modules/

# 废弃方案归档（依赖不存在的 POST /user/tokens，仅作技术记录）
_archive/

# 运行时文件（含敏感信息，切勿提交）
.env
.state
.expire.log

# GitHub App 私钥（极度敏感，绝不能提交）
*.pem
app-private-key.pem

# 编译产物 / 可直接双击运行的部署目录（含 .env 与私钥）
dist/
dist-app/

# 凭证分发链接的剪贴板/调试产物（含 ghs_ 令牌分发地址，切勿提交）
clip-check.txt
*.clip.txt
ghtoken_*
tmp/

# Agent 工作记忆（含内部仓库名、账号、自建服务细节，不公开）
.workbuddy-ai/
